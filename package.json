{
  "private": true,
  "engines": {
    "node": ">=24.17.0"
  },
  "workspaces": ["pkg", "create", "docs", "extensions/vscode-bascik"],
  "scripts": {
    "sync": "yarn workspace bascik-docs generate:llms && yarn workspace create-bascik prepack"
  }
}
