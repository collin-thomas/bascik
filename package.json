{
  "name": "bascik",
  "version": "1.0.0",
  "description": "A Web Framework",
  "main": "transpile.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch ./transpile.js",
    "watch": "npx watch-http-server dist/",
    "debug": "node --inspect --watch transpile.js"
  },
  "author": "collin-thomas",
  "dependencies": {
    "chokidar": "^3.5.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
