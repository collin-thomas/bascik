{
  "name": "bascik-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "author": "Collin Thomas",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "bascik",
    "build": "bascik --build",
    "serve": "bascik --serve",
    "preview": "bascik --serve",
    "e2e": "playwright test --config e2e/playwright.config.ts --reporter=line",
    "pkg-dev": "node --inspect --watch-path=./bascik.config.ts --watch-path=../../pkg/src ../../pkg/src/transpile.ts",
    "generate:llms": "node scripts/generate-llms-txt.ts",
    "generate:og": "node scripts/generate-og-images.ts",
    "generate:search": "node scripts/generate-search-index.ts",
    "lhci": "lhci",
    "lighthouse": "bascik --build && lhci autorun --config=./lighthouse/lighthouserc.json && lhci autorun --config=./lighthouse/lighthouserc.light.json",
    "lighthouse:all": "bascik --build && node lighthouse/generate-config.ts && lhci autorun --config=./lighthouse/lighthouserc.all.json && lhci autorun --config=./lighthouse/lighthouserc.all-light.json",
    "lighthouse:ci": "bascik --build && lhci autorun --config=./lighthouse/lighthouserc.json && lhci autorun --config=./lighthouse/lighthouserc.light.json",
    "typecheck": "tsc --noEmit",
    "unit": "vitest run",
    "test": "vitest run",
    "coverage": "vitest run --coverage.enabled && node scripts/update-coverage.ts",
    "cover": "yarn coverage",
    "update-coverage": "vitest run --coverage.enabled && node scripts/update-coverage.ts"
  },
  "devDependencies": {
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.62.0",
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^24.0.0",
    "@vitest/coverage-v8": "^4.1.10",
    "lighthouse": "^12.4.0",
    "opentype.js": "^2.0.0",
    "sharp": "^0.35.3",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@bascik/bascik": "workspace:*",
    "marked": "^18.0.7"
  }
}
