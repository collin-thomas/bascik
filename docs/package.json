{
  "name": "bascik-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "author": "Collin Thomas",
  "engines": {
    "node": ">=24.17.0"
  },
  "scripts": {
    "dev": "bascik",
    "build": "bascik --build",
    "pkg-dev": "node --inspect --watch-path=./bascik.config.ts --watch-path=../../pkg/src ../../pkg/src/transpile.ts",
    "generate:llms": "node scripts/generate-llms-txt.ts",
    "generate:search": "node scripts/generate-search-index.ts",
    "sync": "node scripts/generate-llms-txt.ts && cp src/pages/assets/SKILL.md ../create/assets/SKILL.md",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@bascik/bascik": "workspace:*",
    "marked": "^18.0.7"
  }
}
